spring:
  application:
    name: CloudConfig
  cloud:
    config:
      server:
        git:
#          uri:/Users/swmoon/Dev/Source/Config/SPRING-CLOUD-SAMPLE-CONFIG
          uri: https://github.com/PaaS-TA/SPRING-CLOUD-SAMPLE-CONFIG.git
          username: changhaksong
          password: ckdgkr96!$
#          basedir: target/config
          force-pull: true


server:
  port: ${PORT:8004}

management:
  security:
    enabled: false
